# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# ⚠️  ATTENTION: Ces valeurs DOIVENT être changées en production !
# Copier vers .env.production et remplir avec de vraies valeurs

# DATABASE
POSTGRES_DB=alliance_manager_prod
POSTGRES_USER=alliance_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE  # Générer avec: openssl rand -base64 32

# NEXTAUTH
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=CHANGE_ME_SECRET_HERE  # Générer avec: openssl rand -base64 32

# OPTIONAL: Si tu utilises un service de DB externe
# DATABASE_URL=postgresql://user:password@host:5432/dbname?schema=public

# MONITORING (optionnel)
SENTRY_DSN=
ANALYTICS_ID= 